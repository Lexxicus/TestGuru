<nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
  <%= link_to t('.test_guru'), root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">  </li>
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <li class="nav-item">
            <a class="nav-link"><%= t('.welcome_admin', all_name: current_user.first_name) %></a> 
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><%= t('.admin_panel') %></a>
            <div class="dropdown-menu">
              <%= link_to t('.tests'), admin_tests_path, class: 'dropdown-item' %>
              <%= link_to t('.create_test'), new_admin_test_path, class: 'dropdown-item' %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link"><%= t('.welcome_user', email: current_user.email)%></a>
          </li>
        <% end %>
        <li class="nav-item"> 
          <%= link_to t('.logout'), destroy_user_session_path, method: :delete, class: 'nav-link' %> 
        </li>
      <% else %>
          <li> <%= link_to t('.signup'), new_user_registration_path, class: 'nav-link' %> </li>
          <li> <%= link_to t('.login'), new_user_session_path, class: 'nav-link' %> </li>
      <% end %>
    </ul>
  </div>
</nav>
