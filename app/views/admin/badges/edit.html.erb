<div class="card">
  <div class="card-header">
    <h2 class="float-left "><%= t('.header', title: @badge.name) %></h2>
  </div>
  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>
<br>
<%= link_to t('helpers.links.back'), admin_badges_path, class: 'btn btn-light' %>
