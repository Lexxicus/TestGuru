<h1><%= t('.header', title: @question.test.title) %></h1>

<p>
  <%= @question.body %>
</p>

<h2><%= t('.answer') %></h2>

<%= link_to t('helpers.links.create_answer'), new_admin_question_answer_path(@question) %>

<%= render @question.answers %>

<%= link_to t('helpers.links.edit'), edit_admin_question_path(@question) %>
<%= link_to t('helpers.links.back'), admin_test_path(@question.test_id) %>
