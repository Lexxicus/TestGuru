<div class="card mb-3">
  <div class="card-header">
    <h2 class="float-left "><%= question_header(@question)%></h2>
  </div>
  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>

<%= link_to t('helpers.links.back'), admin_test_path(@question.test_id), class: 'btn btn-light' %>
