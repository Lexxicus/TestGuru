
<tr>
  <td><%= link_to test.title, test_questions_url(test)%></td>
  <td><%= test.category.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <td>
    <%= link_to 'Show', test_path(test) %>
    <%= link_to 'Edit', edit_test_url(test) %>
    <%= link_to 'Destroy', test_path(test),
                      method: :delete,
                      data: { confirm: 'Are you sure?' } %>
  </td>
</tr>
